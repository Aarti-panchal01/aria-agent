# Environment variables — NEVER push these
.env
.env.local
.env.production

# Streamlit real secrets (the .example is committed)
.streamlit/secrets.toml

# Logs and persistent session data
*.log
.aria_sessions/

# Local screenshot capture scratch (raw frames; the built GIF lives in assets/)
.demo/

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyc
venv/
.venv/
env/
*.egg-info/
.pytest_cache/
.ruff_cache/

# ChromaDB local data (contains personal search history)
memory/chroma_db/

# VS Code
.vscode/

# Output files — contains research history, should not be pushed
output/*.md
output/*.json

# OS files
.DS_Store
Thumbs.db
