# ARIA — Autonomous Research Intelligence Agent
# Runtime dependencies. For dev/UI extras and the console script, prefer:
#   pip install -e ".[dev,ui]"   (see pyproject.toml)

# LangGraph framework
langgraph==1.2.9             # 1.2.9 is compatible with the patched langchain/checkpoint below
langgraph-checkpoint==4.1.1   # >=4.1.1 fixes GHSA-fjqc-hq36-qh5p (unsafe JSON deserialization)

# LangChain ecosystem
langchain==1.3.9          # >=1.3.9 fixes GHSA-gr75-jv2w-4656 (path traversal in loaders)
langchain-core>=1.4.0
langchain-community==0.4.1
langchain-groq==1.1.2

# Structured output
pydantic>=2.0.0

# Memory and storage
# chromadb 1.5.9 is the latest release; GHSA-f4j7-r4q5-qw2c (pre-auth server RCE) has
# no patched version yet. ARIA uses only the embedded PersistentClient — it never runs
# the HTTP server and never sets trust_remote_code — so it is NOT exposed. See SECURITY.md.
chromadb==1.5.9

# Environment configuration
python-dotenv==1.2.2

# Web UI (optional but recommended: streamlit run ui/app.py)
streamlit>=1.30.0
